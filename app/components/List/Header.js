import styled from 'styled-components';

const Header = styled.ul`
  list-style: none;
  margin: 0;
  width: 100%;
  max-height: 30em;
  overflow-y: auto;
  padding: 0 1em;
  font-weight: bold;
  color: green;
  ${'' /* Maybe button color? #41addd */}
`;

export default Header;
